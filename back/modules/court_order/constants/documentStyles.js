/**
 * Document styling constants for court order templates
 * These values must match between template generation and document patching
 */

module.exports = {
    FONT: "Times New Roman",
    SIZE: 24,        // 12pt (Word uses half-points, so 24 = 12pt)
    SIZE_TITLE: 28,  // 14pt
    SIZE_SMALL: 18,  // 9pt
};
